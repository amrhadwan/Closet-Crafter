<!-- res/layout/item_outfit.xml -->

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <ImageView
        android:id="@+id/outfitImageView"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:scaleType="centerCrop"
        android:src="@drawable/mylogo" />

    <TextView
        android:id="@+id/outfitNameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/outfitImageView"
        android:layout_marginStart="16dp"
        android:text="Outfit Name"
        android:textSize="18sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/outfitCategoryTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/outfitNameTextView"
        android:layout_toRightOf="@id/outfitImageView"
        android:layout_marginStart="16dp"
        android:text="Outfit Category"
        android:textSize="14sp" />

    <ImageButton
        android:id="@+id/deleteOutfitButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="?android:attr/selectableItemBackground"
        android:src="@drawable/baseline_delete_forever_24"
        android:contentDescription="Delete Outfit" />
    <include layout="@layout/activity_main_with_navigation" />

</RelativeLayout>
